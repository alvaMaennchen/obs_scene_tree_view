name: Build OBS Scene Tree Folder

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v3
        with:
          version: '6.6.2'
          arch: 'win64_msvc2019_64'

      - name: Download OBS SDK
        run: |
          curl -L -o obs-dev.zip https://github.com/obsproject/obs-studio/releases/download/31.0.0/obs-studio-dev-31.0.0-windows.zip
          7z x obs-dev.zip -oobs-sdk

      - name: Configure CMake
        run: cmake -S . -B build -G "Visual Studio 17 2022" -A x64 -DCMAKE_PREFIX_PATH="${{ env.Qt6_DIR }};${{ github.workspace }}/obs-sdk"

      - name: Build
        run: cmake --build build --config Release

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: obs-scene-tree-folder
          path: build/Release/*.dll
